const tokenInfo = {
  marginRight: '20px',
  overflow: 'auto',
};

const tokenCard = {
  padding: '15px',
  marginTop: '15px',
};

const tokenListItem = {
  marginTop: '5px',
  marginRight: '5px',
  marginLeft: '5px',
  overflow: 'auto',
};

const deployField = {
  width: '60%',
};

const deployRow = {
  marginLeft: 'auto',
  marginBottom: '25px',
};

const tooltip = {
  marginLeft: '15px',
  alignItems: 'center',
  marginTop: 'auto',
  marginBottom: 'auto',
};

export default {
  tokenInfo, tokenCard, tokenListItem, deployField, tooltip, deployRow,
};
