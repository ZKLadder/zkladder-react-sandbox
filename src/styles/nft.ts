const tokenInfo = {
  marginRight: '20px',
  overflow: 'auto',
};

const tokenCard = {
  padding: '15px',
  marginTop: '15px',
};

const tokenListItem = {
  marginTop: '5px',
  marginRight: '5px',
  marginLeft: '5px',
  overflow: 'auto',
};

export default { tokenInfo, tokenCard, tokenListItem };
